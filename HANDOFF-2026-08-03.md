# Handoff: gen-perception 今日进展

Generated: 2026-08-03 Asia/Shanghai
Source scope: 2026-08-03 conversation, repository status documents, local checks, and user-returned server status summary
Repository: `E:\zju-赵州\gen-perception`
Research server project: `/home/jinyankai/gen-perception`

## Executive Summary

- 本地统一感知框架、真实训练/推理基础设施和三类任务接口已经实现；当前工作树的 61 项单测、10 个配置校验、harness smoke、训练 dry-run 和三任务 framework smoke 均通过。
- Stable Diffusion 2 离线资产、ADE20K/NYUv2 样本结构、单卡 CUDA 前向已经由用户在服务器验证。
- 分布式启动超时的已知解决方法是：使用 Conda 环境的精确 Python 路径，以 `python -m torch.distributed.run` 启动，并显式指定静态 `127.0.0.1` rendezvous 和未占用端口；不要回退到裸 `torchrun --standalone`。
- ADE20K 官方 `objectInfo150.txt` 的多词类别名解析问题已经修复；最新服务器回归的 `UNIT_STATUS=0`、`VOCAB_STATUS=0` 证明单测和真实词表校验通过。
- 更新代码后的真实训练回归没有通过：`S005_STATUS=1`，8 卡 DDP 的 NCCL all-reduce 已覆盖 8 个 rank，但在首个 `optimizer_step_completed` 前失败，`S006_STATUS=1`。
- 8 张 GPU 现已被占用。不要重跑、抢占或终止其他任务；当前最优先工作是只读提取失败日志和定位准确 traceback。

## User Preferences and Operating Constraints

- 用户明确要求由用户本人操作研究服务器；Codex 只在本地开发、审查和准备命令，不 SSH、不远程控制服务器。
- 不存储密码、令牌、私钥或其他凭据。
- 数据、模型和输出只使用 `/home/jinyankai/data`、`/home/jinyankai/models`、`/home/jinyankai/outputs`。
- 未完成 codec、真实 forward/backward、短损失、overfit、推理、指标和可视化门禁前，不启动长时间或正式多卡训练。
- 失败实验必须保留并登记；smoke 结果不能表述为正式 benchmark 结果。

## Project State

### Local repository

- Latest committed local HEAD observed: `275b2c0be55b22a2a16d4b13bf86269cf5a41ea9` (`feat: add stage-one data and evaluation gates`).
- 工作树包含大量已修改和未跟踪文件，其中既有本轮实现也可能有用户并行修改。不要执行 `git reset --hard`、`git checkout --`，也不要在未核对范围时批量提交或推送。
- 关键新增/修改模块包括：
  - `scripts/operator/segmentation_training_gate.py`
  - `scripts/operator/distributed_cuda_smoke.py`
  - `scripts/operator/segmentation_ddp_training_gate.py`
  - `perception_diffusion/data/segmentation_vocabulary.py`
  - `perception_diffusion/training/runner.py`
  - `perception_diffusion/inference/runner.py`
  - `docs/run-cookbook.md`
- 标准 `perception_diffusion/training/runner.py` 当前仍是单进程 runner；DDP 只通过独立、受限的 operator gate 验证，不要把 gate 通过等同于正式 runner 已支持 DDP。

### Server environment

- 8 x RTX 4090 24 GB；当前均已被占用。
- Python: `/home/jinyankai/miniconda3/envs/gen-perception/bin/python`
- PyTorch: `2.7.0+cu128`; CUDA wheel: `12.8`
- Model: `/home/jinyankai/models/stable-diffusion-2`
- Model manifest revision: `2511124fabf8bf30c0ca9dccd9729e7f0f2fa669`
- ADE20K: `/home/jinyankai/data/ADEChallengeData2016`
- NYUv2: `/home/jinyankai/data/nyuv2`
- Latest regression artifacts: `/home/jinyankai/outputs/gates/regression_20260803_221718`

## Today’s Completed Work

### Framework and local validation

- 建立三任务统一框架：语义分割、单目深度、表面法向。
- 使用一个共享 SD2 U-Net，以 task token、可选 CLIP text token 和任务条件 adapter 区分任务。
- 实现/完善真实数据加载、codec/evaluator、训练、checkpoint/resume、JSONL/TensorBoard、可选 W&B、推理、条件诊断与 VAE 重建诊断。
- 增加单卡真实 ADE20K backward gate、NCCL scalar all-reduce gate 和 8 卡真实 ADE20K DDP gate。
- 最新本地检查：

| Check | Result |
| --- | --- |
| `python -m unittest discover -s tests -p "test_*.py"` | 61 passed |
| `python scripts/validate_configs.py` | 10 configs validated |
| `python evals/smoke_eval.py` | passed |
| `python scripts/train.py --config configs/smoke.yaml --dry-run` | passed |
| `python scripts/framework_smoke.py --config configs/multitask/stage1_shared_unet.yaml` | passed for segmentation/depth/normal |

### Server validation and fixes

- S002: SD2、ADE20K、NYUv2 离线 CPU 资产/集成验证通过。
- S003: GPU 0 上真实 SD2 CUDA 前向通过，输出有限。
- 初始 8 卡 NCCL 和 Gloo 均在裸 `torchrun --standalone` 路径上超时；问题定位到 launcher/rendezvous，而非仅 NCCL。
- 静态 loopback 启动方案验证可用：精确 Conda Python、`python -m torch.distributed.run`、`127.0.0.1`、独立 master port。
- ADE20K 词表失败被定位为官方五列空白分隔格式与硬编码六字段切分不匹配；动态按 header 宽度解析的修复通过本地回归，最新服务器 `VOCAB_STATUS=0`。

## Latest Updated-Code Regression

User-returned summary:

```text
UNIT_STATUS=0
VOCAB_STATUS=0
S005_STATUS=1
NCCL_STATUS=0
S006_STATUS=1
NCCL_PASS_COUNT=7
DDP_ALLREDUCE_COUNT=8
DDP_STEP_COUNT=0
DDP_PASS_COUNT=0
RESULT_DIR=/home/jinyankai/outputs/gates/regression_20260803_221718
```

Expected configuration from the prepared command:

- Segmentation config: `configs/segmentation/ade20k.yaml`
- Seed: `20260803`
- Precision: FP16
- Resolution: 256 x 256
- S005: one single-GPU optimizer step
- S006: eight ranks, two requested optimizer steps
- Offline model/data access; no checkpoint written

## Run Comparison

| Run boundary | Previous report before code update | Updated-code regression | Current conclusion |
| --- | --- | --- | --- |
| Unit tests | User reported status 0 | `UNIT_STATUS=0` | Relevant local/operator tests pass. |
| ADE20K vocabulary | Parser was previously failing, then repaired | `VOCAB_STATUS=0` | Original duplicate-class-name parser defect is resolved on the server. |
| Single-GPU real backward | User reported `S005_STATUS=0` before updating code; raw log/commit not returned | `S005_STATUS=1` | Updated code regressed or encountered a later runtime/environment failure; exact phase is unknown without log. |
| Isolated 8-rank NCCL | Previous launcher failures were recovered with static rendezvous | `NCCL_STATUS=0`, seven counted pass lines | Launcher/NCCL path is operational, but the missing eighth printed line requires raw-log review. |
| DDP NCCL preflight | Previously user-reported status 0 without retained raw log | Eight `nccl_all_reduce_passed` events | All eight ranks reached and passed the in-gate collective. |
| DDP optimizer | Previously user-reported status 0 without retained raw log | zero optimizer-step and zero final-pass events; `S006_STATUS=1` | Failure occurs after NCCL preflight and before completion of the first optimizer step. |

## Failure Localization

Verified from returned counts:

1. 单元测试和 ADE20K 词表不是这次失败点。
2. 静态 launcher 能启动 8 个 rank。
3. S006 的 8 个 rank 都完成了 NCCL all-reduce preflight。
4. 没有 rank 打印 `optimizer_step_completed`；因此不能声称 DDP backward、参数更新或副本一致性通过。

Still unknown because raw logs were not returned:

- S005 的准确异常类型和失败阶段。
- S006 最后成功的阶段是 `sample_loaded`、`components_loaded`、`ddp_wrapped`、`latents_encoded` 中的哪一个。
- 是否为 OOM、更新后配置/接口不匹配、模型加载问题、某 rank 数据异常、DDP unused-parameter 行为或 backward 异常。
- `NCCL_PASS_COUNT=7` 是 stdout 多 rank 交错导致的漏计数，还是确实缺失一个最终 JSON 行。

Inference, not a verified fact: because `NCCL_STATUS=0` and the later DDP gate recorded eight successful all-reduces, the seven-line isolated-smoke count is more likely a logging/interleaving artifact than NCCL collective failure. Raw log must confirm this.

## Active Blockers

1. **GPU availability:** all eight GPUs are currently occupied; do not retry until explicitly free.
2. **Single-GPU training regression:** updated-code S005 exits 1 despite vocabulary success.
3. **DDP training regression:** S006 reaches all-reduce but no optimizer step completes.
4. **Evidence gap:** server commit, key-file hashes and full trace are expected in the run directory but have not been returned into the repository.
5. **Formal-training gates:** overfit, decoded inference, metric, visualization, checkpoint/resume and multi-seed evidence remain pending.

## Tomorrow’s Required Deliverables (2026-08-04)

Priority order explicitly requested by the user:

### 1. Refine the WK1 report and split the documentation

- 先细化根目录 `REPORT.md` 的 WK1 内容：研究问题、三任务统一框架、Visual Latent Pathway、Task/Text Conditioner、共享 U-Net、数据与评测协议、当前证据、失败项、限制和下一阶段计划。
- 把长篇技术内容从总报告拆到职责清晰的文档中；`REPORT.md` 保留结论、证据摘要和导航链接，避免继续膨胀为实现细节仓库。
- 拆分时复用现有 `docs/unified-perception-framework.md`、`docs/target-encoding-strategies.md`、`docs/real-training-and-inference.md`、`docs/reproduction-plan.md` 和 `docs/run-cookbook.md`，不要复制出相互漂移的重复描述。
- 更新所有受影响的相对链接和 `docs/agent-harness/index.md`，并检查 `STATUS.md`、`BLOCKERS.md`、`EXPERIMENTS.md` 与报告结论一致。
- 完成标准：读者能从 `REPORT.md` 进入各拆分文档；每个实验结论都能追溯到配置、实验 ID 或验证记录；smoke、失败和待验证项有明确标签。

### 2. Complete the three-task VAE reconstruction analysis

- 报告细化和拆分完成后，按 `docs/run-cookbook.md` 第 2 节执行三任务 VAE 重建与保真度分析。
- 该任务需要一张空闲 GPU；当前 8 卡被占用时不得运行或抢占。输出目录必须不存在或为空，失败目录必须保留。
- 预定命令目标：`scripts/analyze_vae_reconstruction.py --task all --precision fp16 --limit 16`，配置为 `configs/multitask/stage1_shared_unet.yaml`，输出到 `$OUTPUT_ROOT/diagnostics/vae-reconstruction-v1`。
- 完成标准：终端报告 `VAE_RECONSTRUCTION_ANALYSIS_COMPLETED`，并生成 `report.json`、`report.csv`、`records.json`、`fidelity.md` 和 segmentation/depth/normal 三任务可视化。
- 分析时必须分别解读公共 encoded-space MSE/MAE/PSNR 和任务指标：分割 IoU/像素准确率、深度 AbsRel/RMSE、法线平均/中位角度误差。不要仅凭公共 MSE 宣称某任务表示最好或最差。
- 需要带回的证据：完整命令与退出码、Git commit、GPU 型号、运行时长、峰值显存、`report.json`、`fidelity.md` 和每任务至少一张重建/误差图。

## Next Concrete Actions

### 1. Retrieve logs without using GPUs

This is read-only and can be run while the GPUs are occupied:

```bash
cd /home/jinyankai/gen-perception
GP_RESULT_DIR=/home/jinyankai/outputs/gates/regression_20260803_221718

cat "$GP_RESULT_DIR/status.log"
cat "$GP_RESULT_DIR/preflight.log"

grep -nE \
  'SEGMENTATION_.*FAILED|Traceback|Error|Exception|out of memory|sample_loaded|components_loaded|ddp_wrapped|latents_encoded|optimizer_step_completed' \
  "$GP_RESULT_DIR/s005r_single_gpu.log" \
  "$GP_RESULT_DIR/s006r_ddp_8gpu.log"

tail -n 200 "$GP_RESULT_DIR/s005r_single_gpu.log"
tail -n 300 "$GP_RESULT_DIR/s006r_ddp_8gpu.log"
```

Return `preflight.log`, the two grep outputs, and both tails. Do not rerun a GPU command yet.

### 2. Diagnose and fix narrowly

- Compare the server commit and key-file hashes in `preflight.log` with the local working tree.
- Identify the first failing stage in S005; fix the single-GPU path before touching DDP-specific code.
- Add a regression test for the exact exception.
- Re-run the 61-test local suite, config validation, dry-run and framework smoke.

### 3. Retry only after GPUs are free

Gate order must remain:

1. S005 single GPU, one 256 px FP16 step.
2. Isolated 8-rank NCCL smoke with exact Conda Python and static loopback rendezvous.
3. S006 8-rank DDP, initially one step; increase to two only after one step passes.

Stop at the first non-zero exit. Do not proceed from failed S005 to S006.

### 4. After the bounded gates pass

- Run three bounded single-task overfit experiments.
- Verify checkpoint resume.
- Produce decoded predictions and task visualizations.
- Run condition-sensitivity diagnostics.
- Only then consider round-robin joint training or formal evaluation.

## Do Not Assume

- Do not assume `S005_STATUS=1` is still the old vocabulary problem; `VOCAB_STATUS=0` contradicts that.
- Do not treat `NCCL_STATUS=0` as proof that DDP training passed.
- Do not treat eight successful DDP all-reduces as proof that backward or optimizer synchronization passed.
- Do not claim a formal experiment, convergence, benchmark metric or full-resolution result.
- Do not assume the local HEAD alone identifies the server code; use the server `preflight.log` commit, status and hashes.
- Do not modify, terminate or preempt the processes currently occupying the eight GPUs.

## Key Evidence Locations

- Server regression directory: `/home/jinyankai/outputs/gates/regression_20260803_221718`
- Launcher recovery: `docs/validation/distributed-launcher-recovery-2026-08-03.md`
- Vocabulary failure/fix: `docs/validation/segmentation-training-gate-vocabulary-failure-2026-08-03.md`
- Server contract: `docs/agent-harness/server-operator-contract.md`
- Run cookbook: `docs/run-cookbook.md`
- Experiment registry: `EXPERIMENTS.md`
