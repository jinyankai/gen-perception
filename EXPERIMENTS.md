# Experiment Registry

Allowed statuses: `PLANNED`, `SMOKE_TEST`, `RUNNING`, `FAILED`, `COMPLETED`, `INVALID`.

| ID | Task | Config | Commit | Device | Status | Smoke signal | Conclusion |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S000 | infrastructure | `configs/smoke.yaml` | `3ea54b8` | CPU | SMOKE_TEST | repository smoke, six-config validation, dry-run passed | Harness and configuration contracts are locally executable. |
| S001 | segmentation/depth/normal structural sharing | `configs/multitask/stage1_shared_unet.yaml` | `3ea54b8` | CPU | SMOKE_TEST | 33 tests passed; finite loss, gradient, and two-step sample per task | Shared interfaces work with tiny injected components; this is not a real SD2 or benchmark result. |
| S002 | stage-one assets and real SD2 integration | `configs/multitask/stage1_shared_unet.yaml` | working tree (commit pending) | CPU | SMOKE_TEST | offline model load; ADE20K/NYUv2 sample checks; finite 8-channel denoiser forward | Required local assets and the real SD2 CPU forward path are usable; no training, GPU, or benchmark result is claimed. |
| S003 | real SD2 CUDA forward | `configs/multitask/stage1_shared_unet.yaml` | server commit unreported | 1 x RTX 4090 (GPU 0) | SMOKE_TEST | user-returned `STAGE1_ASSET_VALIDATION_PASSED`; CUDA 12.8; finite `[1,4,8,8]` output | Single-GPU CUDA component compatibility is verified at 64 px; backward, optimizer, full resolution, and metrics remain unverified. |
| S004 | single-node NCCL all-reduce | N/A | server commit unreported | 8 x RTX 4090 | FAILED | user reported the initial command exceeded 60 seconds; no rank-level output was returned | Distributed runtime is unresolved; rerun the bounded instrumented two-rank and eight-rank smokes before DDP. |
| S004A | static distributed launcher recovery | N/A | server commit unreported | CPU / 2 ranks | SMOKE_TEST | user confirmed the exact-environment static launcher and loopback Gloo commands succeeded; raw rank log was not returned | On this host, use the Conda environment's Python with `-m torch.distributed.run`, explicit `127.0.0.1` master address/unique port, and a loopback interface for single-node rendezvous; NCCL is still pending. |
| S005 | ADE20K real-component training gate | `configs/segmentation/ade20k.yaml` | server commit unreported | 1 x RTX 4090 | FAILED | dataset initialization raised `vocabulary class names must be unique` before model load | Official five-column whitespace metadata was split with one extra field, truncating multiword names and creating false duplicates; no training step ran. |
| S005R | ADE20K real-component training gate retry | `configs/segmentation/ade20k.yaml` | server commit unreported | 1 x RTX 4090 | SMOKE_TEST | user reported `S005_STATUS=0` before a subsequent code update; raw log and code fingerprint were not returned | The parser-fixed path reportedly completed once, but this evidence is qualified and does not cover the updated code. |
| S006 | ADE20K real-component DDP training gate | `configs/segmentation/ade20k.yaml` | server commit unreported | 8 x RTX 4090 | SMOKE_TEST | user reported `S006_STATUS=0` before a subsequent code update; raw rank log and code fingerprint were not returned | The bounded DDP path reportedly completed once, but this is not a formal/full-resolution result and does not cover the updated code. |
| S004B | updated-code single-node NCCL regression | N/A | server fingerprint retained in run directory but not returned | 8 x RTX 4090 | SMOKE_TEST | `NCCL_STATUS=0`, `NCCL_PASS_COUNT=7`; the following DDP gate recorded eight successful all-reduces | Static-rendezvous launcher and NCCL are operational; one missing isolated-smoke pass line remains a raw-log qualification. |
| S005U | updated-code ADE20K real-component training gate | `configs/segmentation/ade20k.yaml` | server fingerprint retained in run directory but not returned | 1 x RTX 4090 | FAILED | `UNIT_STATUS=0`, `VOCAB_STATUS=0`, `S005_STATUS=1` under `/home/jinyankai/outputs/gates/regression_20260803_221718` | The vocabulary fix is verified, but the updated real training path failed at an unknown later stage; traceback is required. |
| S006U | updated-code ADE20K real-component DDP training gate | `configs/segmentation/ade20k.yaml` | server fingerprint retained in run directory but not returned | 8 x RTX 4090 | FAILED | `DDP_ALLREDUCE_COUNT=8`, `DDP_STEP_COUNT=0`, `DDP_PASS_COUNT=0`, `S006_STATUS=1` in the same run directory | All ranks passed the in-gate collective, then the run failed before any optimizer-step event; backward/update/replica consistency remain unverified. |

S002 and S003 are backed by user-returned server logs recorded under `docs/validation/`. S004 records an incomplete/failed smoke; S004A records the user-confirmed launcher recovery with the qualification that its raw rank log was not returned. S004B/S005U/S006U use the user-returned status summary for `/home/jinyankai/outputs/gates/regression_20260803_221718`; exact traces and the server fingerprint still need to be copied from that directory. No formal model experiments have been run.
